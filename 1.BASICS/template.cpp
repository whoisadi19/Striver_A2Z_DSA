#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

void solve() {
    // Your DSA / Competitive Programming code goes here
    
}

int main() {
    // Fast I/O for quick execution
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int t = 1;
    // Uncomment the next line if there are multiple test cases
    // cin >> t; 
    while (t--) {
        solve();
    }
    
    return 0;
}
