/*
for finding number of times an element appears in an array
a brute force apprach would be simply using for loop
for(int i=0; i<n; i++){
if(arr[i] == num){
cnt = cnt+1;
}
return cnt
}

TC = number of numbers to search * O(n)
this is where hashing helps
*/
